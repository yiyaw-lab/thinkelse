{
  "name": "daily-quest-half-hour-production",
  "provider": "external-http-scheduler",
  "reason": "The production Vercel project is on Hobby, where Vercel Cron cannot run more than once per day. Daily quests need a 30-minute trigger so families receive quests during their preferred local hour or half-hour.",
  "request": {
    "method": "GET",
    "url": "https://elsey.app/api/cron/daily-quest",
    "headers": [
      {
        "name": "Authorization",
        "value": "Bearer ${CRON_SECRET}"
      }
    ]
  },
  "schedule": {
    "cron": "0,30 * * * *",
    "timezone": "UTC",
    "description": "Run at the top and half of every hour so half-hour onboarding times fire exactly."
  }
}
